# mutation-test-demo-dev

Dev environment for testing the Olympix mutation test pipeline against the dev API (`api.olympixscantest.com`).

## Workflows

- `olympix-test-dev.yaml` — runs mutation tests against dev API on every push
- `olympix-integrated-securit-dev.yaml` — runs security scan against dev API on every push
